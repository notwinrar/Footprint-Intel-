import { createClient } from '@supabase/supabase-js'import { createClientComponentClient } from '@supabase/auth-helpers-nextjs' // For use in API routes (server-side)export const supabaseAdmin = createClient( process.env.NEXT_PUBLIC_SUPABASE_URL!, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!) // For use in React components (client-side)export const createSupabaseClient = () => createClientComponentClient() export type Plan = 'free' | 'pro' export interface Profile { id: string email: string full_name: string avatar_url: string plan: Plan reports_used: number stripe_customer_id?: string created_at: string} export interface Report { id: string user_id: string org_name: string org_type: string sector?: string region?: string depth: string modules: string[] compare_org?: string report_text?: string prompt?: string word_count?: number status: 'pending' | 'running' | 'complete' | 'error' created_at: string} export const FREE_REPORT_LIMIT = 10
